# Contributing

Thanks for contributing!

## Branch flow

- Work is done on `develop`.
- Open PRs from `develop` to `main`.
- Direct commits to `main` are not accepted.

## Local setup

1. Install dependencies: `npm install`
2. Build or test as needed for your changes.
